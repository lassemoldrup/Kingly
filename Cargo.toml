[package]
name = "kingly"
version = "0.1.0"
edition = "2021"
default-run = "kingly"

[workspace]
members = [".", "lib", "tools"]
default-members = [".", "lib", "tools"]

[dependencies]


[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
